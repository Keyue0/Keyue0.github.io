# Bing Auto · 自动复盘版（GitHub Actions 驱动）

> 爱在冰川短线情绪周期框架 · 每日两次自动更新（12:00 盘中快照 + 16:00 收盘正式）· 支持追溯补跑

**在线访问**：`https://keyue0.github.io/auto/`

## 自动更新机制

| 时段 | 时间（北京时间） | 行为 | 产物 |
|---|---|---|---|
| **am 盘中** | 每天 12:00 | 拉取盘中实时数据，生成盘中快照 | `data/reviews/<日期>_am.json`（存档，前端默认不展示） |
| **pm 收盘** | 每天 16:00 | 拉取收盘定型数据，生成正式全套 | `data/reviews/<日期>.json` + sentiment/fund/ladders/pools/lhb（前端展示） |

- 仅交易日（周一~周五）触发；非交易日自动跳过（脚本会回溯最近交易日）
- **每次运行都自动追溯最近 1 个交易日**（`--lookback=1`）：即使某天漏跑，第二天 12:00/16:00 会自动补上前一天
- GitHub Actions 云端运行，**与本机无关，电脑关机照常更新**

## 追溯补跑

### 方式一：自动追溯（推荐，每天自动）
- workflow 内置 `--lookback=1`：每次跑"昨天 + 今天"两天
- 想要更多追溯：GitHub Actions 手动触发时填 `lookback=3` → 补最近 4 个交易日

### 方式二：手动触发
1. 打开仓库 GitHub 网页 → **Actions** 标签
2. 左侧选 **"Bing Auto · 每日双定时更新"** → 右侧 **Run workflow**
3. 填写：
   - `slot`：`am`=盘中快照 / `pm`=收盘正式（默认 pm）
   - `lookback`：追溯最近 N+1 个交易日（默认 1）
   - `date`：指定交易日 `YYYY-MM-DD`（留空自动取最近交易日）
4. 点绿色 **Run workflow**，约 30 秒后完成

**追溯"一天的两次记录"示例**：
```bash
node scripts/fetch_data.js --date=2026-08-20 --slot=am   # 该日盘中快照 → 2026-08-20_am.json
node scripts/fetch_data.js --date=2026-08-20 --slot=pm   # 该日收盘正式 → 2026-08-20.json
node scripts/fetch_data.js --slot=pm --lookback=1        # 昨天+今天 收盘正式（自动补漏）
```

## 本地手动运行

```bash
cd auto
node scripts/fetch_data.js                              # 收盘正式（自动取最近交易日）
node scripts/fetch_data.js --slot=am                    # 盘中快照
node scripts/fetch_data.js --slot=pm --lookback=2       # 追溯最近 3 个交易日收盘版
node scripts/fetch_data.js --date=2026-08-20 --slot=am  # 指定某天盘中快照
```

## 数据源（公开免费，无需申请）

| 数据 | 接口 |
|---|---|
| 涨停/跌停/连板池 | 东财 `push2ex.getTopicZTPool / getTopicDTPool` |
| 大盘指数 | 腾讯 `qt.gtimg.cn`（GBK 解码） |
| 涨跌家数 | 东财 `push2delay ulist f104/105/106` |
| 板块资金流 | 东财 `push2delay clist f62`（60 行业板块） |
| 龙虎榜 | 东财 `datacenter RPT_DAILYBILLBOARD_DETAILSNEW` |
| **龙虎榜席位明细**（买卖营业部/游资识别/拉萨） | 东财 `datacenter RPT_BILLBOARD_DAILYDETAILSBUY/SELL` |
| **板块历史K线**（120日回撤/20日涨幅/超额） | 东财 `push2his fflow/daykline`（f62 收盘点位，多域名重试） |
| **近5日板块涨停家数** | 东财涨停池回溯聚合（复用交易日回溯） |
| **昨日涨停表现** | 昨日涨停池 + 腾讯批量行情（平均涨跌/红盘率/大面率） |

## 🧠 大模型深度分析（LLM 可选）

## 🧠 大模型深度分析（LLM 可选，推荐开启）

默认规则化模板生成九段。**配置 LLM 后升级为 AI 深度分析**（九段全部由大模型思考：局势判断/为什么/明确方向/精准止损价/丰富板块/龙虎榜解读）：

### 配置（GitHub Secrets）

仓库 **Settings → Secrets and variables → Actions → New repository secret** 添加：

| Name | Value | 说明 |
|---|---|---|
| `DEEPSEEK_API_KEY` | `sk-...` | **DeepSeek API Key**（两者填一个即可，脚本兼容） |
| `LLM_API_KEY` | `sk-...` | 备选命名（与 DEEPSEEK_API_KEY 二选一） |
| `LLM_MODEL` | `deepseek-v4-flash` | 可选，默认就是它（深度思考模式） |
| `LLM_BASE_URL` | `https://api.deepseek.com/chat/completions` | 可选，默认就是这个 |

> DeepSeek 申请：https://platform.deepseek.com（充 10 元够用很久）。模型 `deepseek-v4-flash` 自动启用 thinking 深度思考。

### 开启后每天自动产出

- **九段全部 AI 分析**（每段数据下方追加 `✨ AI 深度分析`）：局势判断、为什么走成这样、明日推演
- **候选池 3-6 只**：每只精准买点/止损/目标价（止损-3%~-5%、目标+8%~+15%，基于收盘价推算）+ 推荐逻辑 + 触发条件 + 风险提示
- **龙虎榜盘面解读**：LLM 生成的 3 条核心观察
- 未配置 key 或调用失败 → 自动回退规则模板，不影响任何功能

## 🔄 跨日积累机制（对齐冰川框架）

| 能力 | 实现 | 说明 |
|---|---|---|
| **资金三分类** | `classifyFundTrend` 用资金流日线 5 日每日序列 | 首次流入 / 连续流入 / 前期流出后回补（对齐 skill 语义） |
| **跨日资金库** | `data/capital_history.json`（随仓库提交） | 每交易日 60 板块资金存档，跨 run 积累，供历史对比 |
| **次日回头看** | `data/lookback/<日期>.json` + 复盘第2段 | 读前日复盘"明日计划"→ 验证放弃条件触发/目标板块资金 → 生成结论 |
| **放弃条件追踪** | 同上 | 上证阈值/跌停>20/涨停<30 逐项验证，触发即提示空仓 |

## 🔗 WorkBuddy 增强通道（通达信口径，更准）

东财历史接口在云端自动版可用，但**通达信口径更准**（与本地冰川复盘一致）。
自动版脚本支持**增强数据优先覆盖**：只要 `auto/data/sectors/enhanced_<日期>.json` 存在，第 6 段就用它（通达信口径），否则回退东财自动扫描。

### 生成方法（在 WorkBuddy 里，连接通达信 MCP 后）

1. 拉取板块指数日K（130 根）：
   ```
   tdx_lookup_stock(query="有色金属", range="ZS") → 000819
   tdx_kline(code="000819", setcode="1", period="4", wantNum="130")
   tdx_kline(code="000300", setcode="1", period="4", wantNum="130")  # 沪深300 基准
   ```
   （沪深300 拉一次；板块可多拉：有色/电子/通信/医药/机器人/CPO 等）
2. 把返回 JSON 保存到 `auto/scripts/raw_kline/`（如 `000819_2026-08-21.json`）
3. 运行生成：
   ```bash
   cd auto
   node scripts/build_enhanced.js --date=2026-08-21
   ```
4. 生成的 `data/sectors/enhanced_<日期>.json` 随仓库推送 → fetch_data.js 自动优先使用

> 已内置示例：`enhanced_2026-08-21.json`（有色金属 000819 通达信真实数据：120日回撤 -22.9% / 20日涨幅 +11.7%，与本地复盘口径一致）。完整多板块版可在 WorkBuddy 里按上述流程批量生成。

## ✅ 九段数据覆盖矩阵（完整性审计 2026-08-21 实测）

| 九段 | 数据项 | 接口来源 | 与本地通达信对照 |
|---|---|---|---|
| 1 大盘情绪 | 指数点位/涨跌/成交额 | 腾讯 `qt.gtimg.cn` | ✅ 上证3905.20 +0.04% 完全一致 |
| 1 大盘情绪 | 涨跌家数 | 东财 `push2delay ulist` | ⚠️ 2431/2663 vs 2505/2862（差~3%） |
| 1 大盘情绪 | 涨停/跌停家数 | 东财涨停池/跌停池 | ⚠️ 54/13 vs 57/14（ST/科创板口径差3-5家） |
| 1 大盘情绪 | 连板梯队/最高板 | 东财涨停池 `lbc` | ✅ 3板 一致 |
| 1/4 | **昨日涨停表现** | 昨日涨停池+腾讯批量行情 | ✅ 方向一致（-0.73% vs -0.48%，均亏钱） |
| 2 次日回头看 | 昨日涨停验证 | 同上 | ✅ 真实数据，非占位 |
| 3 板块效应 | 涨停板块分布/龙头 | 东财涨停池 `hybk` 聚合 | ⚠️ 板块分类口径略异（东财行业标签 vs 通达信题材） |
| 4 龙头梯队 | 高度板/连板明细 | 东财涨停池 | ✅ |
| 5 主力资金 | 当日净流入/流出Top5 | 东财 `clist f62` | ⚠️ **金额口径差异大**（如有色 +88.29 vs 通达信 +9.35，东财为板块级主力口径） |
| 5 主力资金 | **近5日资金趋势** | 东财资金流日线 `f52` 累加 | ✅ 持续流入/流出/转向分类 |
| 6 低位右侧 | **120日回撤/20日涨幅/超额** | 东财资金流日线 `f62` 收盘点位 / 通达信 enhanced | ✅ 有色 -22.9%/+11.7% 与本地一致 |
| 6 低位右侧 | **近5日涨停家数** | 东财涨停池回溯聚合 | ✅ |
| 7 共振背离 | 资金+情绪+家数 | 规则化 | ✅ 判定逻辑一致 |
| 8 明日计划 | 仓位/目标/放弃条件/候选池 | 规则化+三重共振 | ⚠️ 候选池仅列"三重共振"板块（保守） |
| 9 一句话 | 情绪收尾 | 规则化 | ✅ |

### 口径差异说明（诚实标注）

1. **涨停/跌停家数**：东财涨停池（54/13）与通达信 tdx_screener（57/14）差 3-5 家——ST、科创板、北交所口径不同，已每段标注来源
2. **主力资金金额**：东财板块资金流（f62，板块级主力净额）与通达信 zjlx 代表股口径差异大——**数值仅供参考方向，绝对值以本地通达信为准**
3. **板块分类**：东财 `hybk` 行业标签 vs 通达信题材板块，聚合结果略有差异
4. **候选池**：自动版只列"三重共振"（低位+资金+情绪全通过）板块，比本地精修版更保守——宁可少列不滥列
5. 所有数据均为**真实行情接口**返回，无编造；差异均标注口径，精修以本地冰川复盘（通达信）为准

## 与精修版（stock-web/）的关系

- `stock-web/`：本地通达信精修版（数据最准、解读最深，人工维护）
- `auto/`：云端自动版（零维护、每天两次，数据为东财口径，略逊于通达信）
- 两者**独立共存，互不影响**；精修版也可覆盖 `auto/data/reviews/<日期>.json` 后推送，自动版优先展示精修内容

## 免责声明

⚠️ 框架化复盘/推演，数据来源东财/腾讯公开行情接口，**不构成投资建议**。盘中快照数据为未定型快照，仅供参考。
